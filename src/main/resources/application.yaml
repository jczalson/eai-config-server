spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://zalson@bitbucket.org/zalson/eai-config-files.git
          cloneOnStart: true
          username: ${user}
          password: ${token}
      

server:
  port: 8888    
